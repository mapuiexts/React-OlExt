import React from "react";

/**
 * The Map Context to globablly store the map.
 * 
 * @author [Paulo ARAGAO] (https://github.com/PauloSSAragao)
 */
const MapContext = new React.createContext(null);

export default MapContext;

